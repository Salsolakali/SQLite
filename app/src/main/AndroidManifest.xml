<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.fernana6.sqlite">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".activities.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".activities.ViewUsersActivity" />
        <activity android:name=".activities.RegisterUsersActivity" />
        <activity android:name=".activities.ViewUsersListViewActivity" />
        <activity android:name=".activities.SpinnerViewUsersActivity" />
        <activity android:name=".activities.IndexActivity" />
        <activity android:name=".activities.RegisterNewUserActivity"></activity>
    </application>

</manifest>